# Plans

Implementation plans produced by the writing-plans flow. Each file is named `YYYY-MM-DD-<feature>-plan.md`.

**Convention:** plans are written from specs, committed to `main` immediately (not the feature branch), and consumed once implementation begins. To supersede a plan, create a new one and set `status: superseded` in the old one's frontmatter — never edit a consumed plan.
